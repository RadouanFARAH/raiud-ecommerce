import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.raiud',
  appName: 'RAIUD E-commerce',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
